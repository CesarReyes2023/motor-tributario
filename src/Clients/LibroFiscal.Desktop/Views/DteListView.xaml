<UserControl x:Class="LibroFiscal.Desktop.Views.DteListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="DTE Emitidos" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="Historial de Documentos Tributarios Electrónicos" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,5,0,0"/>
            </StackPanel>
            
            <Button Grid.Column="1" Content="Nuevo DTE" Command="{Binding NavigateToCreateDteCommand}" Style="{StaticResource PrimaryButton}" VerticalAlignment="Center"/>
        </Grid>

        <!-- DataGrid / List -->
        <Border Grid.Row="1" Background="{DynamicResource BgAppBrush}" CornerRadius="8" BorderBrush="{DynamicResource BgBorderBrush}" BorderThickness="1" Padding="20">
            <!-- DataGrid nativo requiere mucho styling para verse "dark". Usaremos un DataGrid estándar con styling ligero por ahora o un ListView -->
            <ListView ItemsSource="{Binding Dtes}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Fecha Emisión" DisplayMemberBinding="{Binding FechaEmision, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                        <GridViewColumn Header="Número de Control" DisplayMemberBinding="{Binding NumeroControl}" Width="150"/>
                        <GridViewColumn Header="Sello Recepción" DisplayMemberBinding="{Binding SelloRecepcion}" Width="150"/>
                        <GridViewColumn Header="Cliente" DisplayMemberBinding="{Binding Cliente}" Width="180"/>
                        <GridViewColumn Header="Total" DisplayMemberBinding="{Binding Total, StringFormat=C}" Width="100"/>
                        <GridViewColumn Header="Estado" DisplayMemberBinding="{Binding Estado}" Width="100"/>
                        <GridViewColumn Header="Acciones" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Ver PDF" 
                                            Command="{Binding DataContext.ExportToPdfCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" 
                                            Style="{StaticResource PrimaryButton}" 
                                            Padding="10,5" FontSize="12"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
