<UserControl x:Class="LibroFiscal.Desktop.Views.UsersManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:LibroFiscal.Desktop.Views"
             xmlns:viewmodels="clr-namespace:LibroFiscal.Desktop.ViewModels"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=viewmodels:UsersManagementViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Glassmorphism Panel Style -->
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Glass TextBox Style -->
        <Style x:Key="GlassTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0FFFFFFF"/>
            <Setter Property="BorderBrush" Value="#44FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <b:Interaction.Triggers>
            <b:EventTrigger EventName="Loaded">
                <b:InvokeCommandAction Command="{Binding LoadDataCommand}"/>
            </b:EventTrigger>
        </b:Interaction.Triggers>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: User List -->
        <Border Style="{DynamicResource GlassPanel}" Padding="20" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Usuarios Registrados" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Users}" 
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          Background="Transparent"
                          RowBackground="#0FFFFFFF"
                          AlternatingRowBackground="#1AFFFFFF"
                          Foreground="Black"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          BorderThickness="0">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2AFFFFFF"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Rol" Binding="{Binding Role}" Width="100"/>
                        <DataGridTextColumn Header="Empresas Asignadas" Binding="{Binding AssignedCompaniesCount}" Width="150"/>
                        <DataGridCheckBoxColumn Header="Activo" Binding="{Binding IsActive}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Right Column: Admin Actions -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Create User Card -->
            <Border Style="{DynamicResource GlassPanel}" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Crear Nuevo Usuario" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Nombre de Usuario:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                    <TextBlock Text="Contraseña:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                    <!-- Ponytail: PasswordBox bind no es simple en WPF sin hacks o dependencias extras, usando TextBox para simplificar la demo SuperAdmin -->
                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource GlassTextBox}" Margin="0,0,0,20"/>

                    <Button Content="Crear Usuario" Command="{Binding CreateUserCommand}" Style="{StaticResource ModernButton}"/>
                </StackPanel>
            </Border>

            <!-- Assign Company Card -->
            <Border Grid.Row="1" Style="{DynamicResource GlassPanel}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Asignar Empresas" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Grid.Row="1" Text="{Binding SelectedUser.Username, StringFormat='Usuario seleccionado: {0}', FallbackValue='Seleccione un usuario de la lista'}" 
                               Foreground="#FFAA00" FontWeight="SemiBold" Margin="0,0,0,15" TextWrapping="Wrap"/>

                    <!-- Companies List -->
                    <ListBox Grid.Row="2" ItemsSource="{Binding Companies}" Background="Transparent" BorderThickness="0" Margin="0,0,0,10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding LegalName}" Foreground="White" VerticalAlignment="Center"/>
                                    <!-- Ponytail: Pass CompanyId as command parameter -->
                                    <Button Grid.Column="1" Content="Asignar" Margin="10,0,0,0" Padding="10,5"
                                            Command="{Binding DataContext.AssignCompanyCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding Id}"
                                            Background="#007ACC" Foreground="White" BorderThickness="0">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
