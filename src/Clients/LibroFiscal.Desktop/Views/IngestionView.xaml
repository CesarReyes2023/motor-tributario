<UserControl x:Class="LibroFiscal.Desktop.Views.IngestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:LibroFiscal.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:IngestionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Ingesta Masiva de Facturas" FontSize="28" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Arrastra y suelta tus archivos JSON de Hacienda o imágenes escaneadas." FontSize="14" Foreground="#6B7280" Margin="0,0,0,20"/>

        <!-- Drag & Drop Zone -->
        <Border Grid.Row="2" BorderBrush="#3B82F6" BorderThickness="2" CornerRadius="10" Background="#EFF6FF" AllowDrop="True" Drop="Border_Drop">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,30,0,10">
                    <TextBlock Text="📁" FontSize="64" HorizontalAlignment="Center" Foreground="#93C5FD"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="18" FontWeight="Medium" Foreground="#1E40AF" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- DataGrid for Dropped Files -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding FilesToProcess}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" IsReadOnly="True" Background="Transparent" BorderThickness="0"
                          HeadersVisibility="Column" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Archivo" Binding="{Binding FileName}" Width="*"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Status}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Procesado">
                                            <Setter Property="Foreground" Value="#10B981"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Pendiente">
                                            <Setter Property="Foreground" Value="#F59E0B"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Error">
                                            <Setter Property="Foreground" Value="#EF4444"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="#6B7280" FontStyle="Italic"/>
            
            <Button Content="Limpiar" Command="{Binding ClearFilesCommand}" Background="#F3F4F6" Foreground="#374151" 
                    Padding="20,10" FontWeight="SemiBold" BorderThickness="0" Margin="0,0,10,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="Procesar y Guardar" Command="{Binding ProcessFilesCommand}" Background="#10B981" Foreground="White" 
                    Padding="20,10" FontWeight="SemiBold" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
